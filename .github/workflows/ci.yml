name: ci
on: [push, pull_request]
jobs:
  old:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/setup-go@v2
        with:
          go-version: '~1.15'
      - name: go verion
        run: |
          go version
          go env
  new:
    runs-on: ubuntu-20.04
    steps:
      - run: docker pull debian:9
      - name: setup go
        run: |
          echo "PATH=$GOROOT_1_15_X64/bin:$PATH" >> $GITHUB_ENV
          echo "GOROOT=$GOROOT_1_15_X64" >> $GITHUB_ENV
      - name: go verion
        run: |
          go version
          go env
